@{
    ViewData["Title"] = "Select Material";
}

<link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.7/dist/css/bootstrap.min.css" rel="stylesheet" />
<style>
    .material-card {
        background: #ffffff;
        border-radius: 16px;
        box-shadow: 0 8px 24px rgba(0, 0, 0, 0.08);
        padding: 2rem;
        margin-top: 2rem;
        transition: transform 0.2s;
        max-width: 600px;
        margin: 2rem auto;
    }

    .material-card:hover {
        transform: scale(1.01);
    }

    .form-select:focus {
        border-color: #e80266;
        box-shadow: 0 0 0 0.25rem rgba(232, 2, 102, 0.25);
    }

    .form-label {
        font-weight: 500;
        color: #333;
    }

    .btn-custom {
        background-color: #e80266;
        border: none;
    }

    .btn-custom:hover {
        background-color: #c20158;
    }

    .section-header {
        font-size: 1.8rem;
        font-weight: 600;
        color: #343a40;
    }
</style>

<div class="material-card w-100">
    <h2 class="text-center section-header">🔧 Select Material</h2>
    <p class="text-center text-muted mb-4">
        Choose the material for your robot arms. This will determine the mechanical properties used in calculations.
    </p>

    <form method="post" action="/Robotic/ForceInput">
        <input type="hidden" name="robotType" value="@ViewBag.RobotType" />
        <input type="hidden" name="numberOfArms" value="@ViewBag.NumberOfArms" />

        <div class="mb-4">
            <label for="material" class="form-label">🏗️ Material Type</label>
            <select class="form-select form-select-lg" id="material" name="material" required>
                <option value="">Select a material...</option>
                <option value="steel">🔩 Steel (E=200 GPa, ρ=7850 kg/m³)</option>
                <option value="aluminum">⚡ Aluminum (E=70 GPa, ρ=2700 kg/m³)</option>
                <option value="titanium">🛡️ Titanium (E=116 GPa, ρ=4500 kg/m³)</option>
                <option value="carbonFiber">🎯 Carbon Fiber (E=230 GPa, ρ=1600 kg/m³)</option>
                <option value="brass">🟡 Brass (E=105 GPa, ρ=8500 kg/m³)</option>
                <option value="copper">🟠 Copper (E=110 GPa, ρ=8960 kg/m³)</option>
            </select>
        </div>

        <div class="text-center">
            <button type="submit" class="btn btn-custom px-4 py-2" id="next-btn" disabled>
                🚀 Next: Enter Force
            </button>
        </div>
    </form>
</div>

<script>
    document.getElementById('material').addEventListener('change', function() {
        const nextBtn = document.getElementById('next-btn');
        nextBtn.disabled = !this.value;
    });
</script> 